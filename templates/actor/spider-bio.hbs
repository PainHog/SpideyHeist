<section class="tab heisty-tab bio-tab {{tabs.bio.cssClass}}" data-tab="bio" data-group="primary">

  <div class="panel">
    <h3 class="panel-title">Biography</h3>
    {{#if editable}}
    <prose-mirror name="system.biography" toggled document-uuid="{{actor.uuid}}"
                  value="{{system.biography}}">{{{enrichedBio}}}</prose-mirror>
    {{else}}
    <div class="prose">{{{enrichedBio}}}</div>
    {{/if}}
  </div>

  <div class="panel">
    <h3 class="panel-title">The Waiting Web <span class="panel-sub">your backup spider, ready to draw</span></h3>
    {{#if editable}}
    <prose-mirror name="system.details.waitingWeb" toggled document-uuid="{{actor.uuid}}"
                  value="{{system.details.waitingWeb}}">{{{enrichedWaitingWeb}}}</prose-mirror>
    {{else}}
    <div class="prose">{{{enrichedWaitingWeb}}}</div>
    {{/if}}
  </div>

  <div class="panel">
    <h3 class="panel-title">Notes</h3>
    {{#if editable}}
    <prose-mirror name="system.details.notes" toggled document-uuid="{{actor.uuid}}"
                  value="{{system.details.notes}}">{{{enrichedNotes}}}</prose-mirror>
    {{else}}
    <div class="prose">{{{enrichedNotes}}}</div>
    {{/if}}
  </div>
</section>
