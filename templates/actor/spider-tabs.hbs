<nav class="tabs sheet-tabs heisty-tabs" data-group="primary">
  {{#each tabs}}
    <a class="tab {{this.cssClass}}" data-tab="{{this.id}}" data-group="{{this.group}}">
      <i class="{{this.icon}}"></i> <span>{{localize this.label}}</span>
    </a>
  {{/each}}
</nav>
